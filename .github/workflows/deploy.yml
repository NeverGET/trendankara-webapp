name: Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            # Setup Git credentials for private repo
            cd /opt/app

            # Clone with token if doesn't exist
            if [ ! -d ".git" ]; then
              git clone https://${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git .
            else
              git config pull.rebase false
              git pull https://${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git main
            fi

            # Build and run with Docker
            docker build -t radioapp .
            docker stop radioapp || true
            docker rm radioapp || true

            # Run the container
            docker run -d \
              --name radioapp \
              --network radio-network \
              -p 3000:3000 \
              --restart always \
              radioapp

            # Clean up
            docker image prune -f
